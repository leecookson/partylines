
const _ = require('lodash');

const profile = require('../util/profile');

module.exports = {

  /*
   *    options:
   *      separation: minimum degree of seapration
   *      extreme: average degree of extremism (0 is balanced-not possible, 0.5 - average deviation from balance is .5)
   *
   */
  validateParties: (parties, options) => {
    _.each(parties, (party) => {

    });
  }

};
